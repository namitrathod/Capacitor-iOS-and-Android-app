/**
 * Single place for the backend API base (path must match FastAPI, e.g. /api/v1).
 * Change here only; dev (`ng serve`) and prod builds both use this.
 */
export const API_BASE_URL = 'https://app.scriptchain.co/api/v1';
