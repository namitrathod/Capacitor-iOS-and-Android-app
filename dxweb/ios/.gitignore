# Build / IDE noise
App/build
App/output
DerivedData
xcuserdata

# CocoaPods — not committed by default (large). After first `pod install` on a Mac you may
# commit `App/Pods/` if you want clone-and-open-Xcode with zero terminal commands.
App/Pods

# Tracked on purpose (after `npm run build` + `npx cap sync ios`):
# - App/App/public          → bundled web assets
# - App/App/capacitor.config.json, config.xml → sync output
